export const MAIN_MENU = [
  [[`Config your own instance`, 'act:own-instance']],
  [[`Choose ready workflow`, 'act:ready-workflow:select']],
  [[`some test action`, 'act:some']],
] as [string, string][][]